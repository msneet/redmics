<%# redmics - redmine ics export plugin, Copyright (c) 2010-2011 Frank Schwarz, frank.schwarz@buschmais.com %>
<%
  user = User.current
%>
<% if user.allowed_to?(:view_calendar, project, :global => true) %>
  <h3><%= l(:label_icalendar_header) %></h3>
  <% unless user.anonymous? %>
  <%= link_to l(:label_issues_assigned), { :controller => 'i_calendar', :assignment => :my, :status => 'open', :action => 'index', :project_id => project, :key => user.rss_key, :format => 'ics' }, :title => l(:toolip_icalendar_link) %> 
  <br/>
  <% end %>
<% end %>
